//Format of the config
// Copy it in config.js, after inputing the required data

config = {
    API : {
        PORT : PORT_NO,
    },
    FRONTEND : {
        PORT : PORT_NO
    },
    SITE : SITE_URL
}


module.exports = config